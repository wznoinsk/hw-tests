tempest-lib>=0.13.0
oslo.concurrency>=2.3.0
oslo.log>=1.6.0
# http://libguestfs.org/download/python/guestfs-1.34.2.tar.gz # may be required if Tempest/tests don't have access to system-wide libraries where guestfs is normally installed (i.e.: when not run as Tempest plugin)
pyserial
